; PHP-FPM / CGI hosts often ignore .htaccess php_value — use this instead.
; Registration submits several documents at once; post_max_size must cover the total.
upload_max_filesize = 10M
post_max_size = 64M
max_execution_time = 300
max_input_time = 300
memory_limit = 256M
